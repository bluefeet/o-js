var o = require('../o');
var test = require('tap').test;

test( 'basic', function (t) {
    // TODO (not really testable in node )
/*
    t.equal(oJS, o, 'oJS is exported');
    var oldO = o;
    var newO = o.noConflict();
    t.equal(o, 'ORIGINAL_O', 'originl value of o was set');
    t.equal(oldO, oJS, 'returned o');
    t.end();
*/
    t.end();
});
